/// <reference types="cypress" />

const { navigateTo } = require("../support/page_objects/navigationPage")

describe('Hooks page objects model', () => {

    beforeEach('Open application', () => {
        cy.visit('/')
    })

    it('Verify navigation accross the page', () => {
        
    })
    

})